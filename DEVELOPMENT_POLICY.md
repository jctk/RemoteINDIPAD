# 開発方針書

## 1. 目的

本プロジェクトの目的は、次の2点を実現することである。

1. Windows に接続されたゲームパッド入力をネットワーク経由で StellarMate へ送信する。
2. StellarMate など Linux 上の Ekos または INDI を操作し、望遠鏡制御の入力を実行する。

本件は、Windows 環境でのゲームパッド操作性を維持しつつ、Linux 側で一貫した制御に変換することを重視する。特に、観測時の低遅延、小さな設定変更、簡易な運用を前提に設計する。

## 2. 実行環境

### 2.1 送信側 Windows 環境

- OS: Windows 11
- Python 3.10 以降を想定
- 対応入力: 一般的な DirectInput / XInput 対応ゲームパッドを対象とする。ELECOM JC-U3712T をリファレンスとする。
- 実行形態: Python スクリプトの手動起動

#### 2.1.1 リファレンスとするゲームパッドの入力構成

- デバイス: ELECOM JC-U3712T
- 入力機構: 十字キー 1 個
- アナログスティック: 2 個
- ボタン: 12 個
- 概要: 標準的な HID / Game Controller として認識される想定で、Python の `pygame.joystick` から取得する。
- 実装上の前提: ボタンは `button_0` 〜 `button_11` のようなインデックス表現に正規化する。
- 接続デバイスの選択方針: ゲームパッドが 1 台のみ接続されている場合は自動選択し、対話入力を行わない。複数接続時は一覧を表示して選択させるか、`--device` で明示指定する。

### 2.2 受信側 Linux 環境

受信側は Linux ベースの環境を対象とする。

- OS: Linux。リファレンスとする OS は StellarMate OS 2.x および Ubuntu 26
- Python 3 環境
- KStars / Ekos / INDI が動作していることを前提とする。
- 通信は LAN 上で実行される。
- 受信側スクリプトは OS に依存しにくい構成にし、Linux 環境で共通動作させる。

### 2.3 実行形態の基本方針

- 送信側: Windows 上でゲームパッド入力を取得し、抽象化操作に変換しネットワークで送信する。
- 受信側: Linux 環境で抽象化操作を受け、KStars / Ekos / INDI の制御コマンドに変換してコントロールする。

## 3. 全体構成

本プロジェクトは、以下の2本の Python スクリプトを中心に構成する。

1. Windows 側スクリプト
    - ゲームパッド入力の継続的な取得
    - 入力値の抽象化操作への変換
    - 抽象化操作をネットワーク経由で低遅延で Linux へ送信
    - UIはpyside6を用いたGUIを採用

2. Linux 受信側スクリプト
    - 抽象化操作のネットワーク経由での受診
    - 抽象化操作を解釈しKStars / Ekos / INDI への制御コマンド変換
    - 通信が途切れた際に受信側は継続すると危険動作を停止
    - UIはpyside6を用いたGUIを採用

- この構成により、Windows では入力取得から抽象化された操作への変換に特化し、Linux 受信側では制御ロジックと観測機器制御に特化させる。
- 受信側は Linux（StellarMate OSやUbuntu）のユーザー環境へ導入する。（StellarMate OS はカスタマイズが限定された Arch Linux であり pacmanの実行に制限があるためシステム全体へコマンドの追加は難しい。）
- 開発を容易にするため受信側は Windows 上でも実行可能とする。ただし KStars / Ekos / INDI の制御は行わない。

### 3.1 GUI - GUI定義表の見方

- GUI定義表はGUIの構成要素について定義する。
- アプリ Windowのサイズは可変。Windowのリサイズによりコンソールはリサイズされる。そのほかの要素は列位置は調整されるが、行位置は変わらない。
- 位置（行）は上の要素からのオフセット。上の要素と同じ行に表示する場合は 0。一要素分下に表示する場合は +1。
- 位置（列）は行の中での水平位置。上の要素と同じ行に含まれる場合は 位置（行） が 0 になる。
- 位置（列）が空欄の場合は同じ行の直前の要素のすぐ右側。
- 位置（列）が "全体"の場合は Window の左端から右端までの全体。

### 3.2 GUI - Windows

- Windows 側スクリプトのGUI定義表の以下の通り。
- ウィンドウタイトルは "INDIPAD"。
- "設定ファイル" 列に "保存" と書かれている項目は、"接続" および "閉じる" を実行時に設定ファイルに保存し、次回起動時に設定ファイルに保存さえている値を初期値として使用する。

| 位置（行） | 位置（列） | 名称 | 種類 | 説明 | 初期値 | 設定ファイル |
| - | - | - | - | - | - | - |
| 0 | 左寄せ | コントローラー | ラベル+ドロップダウンリスト | ラベルと選択可能なコントローラー名のリスト | 最初に検出されたデバイス | 保存 |
| +1 | 左寄せ | ホスト / ポート | ラベル+2つのテキスト | 接続先ホスト名 または IP アドレスと整数値 | localhost / 50007 | 保存 |
| +1 | 左寄せ | ハートビートのログ出力 | チェックボックス+ラベル | チェックボックスの後ろにラベル表示 | オフ | 保存 |
| +1 | 左寄せ | フォーカサーステップ | ラベル+テキスト（スピンボタン付き） | フォーカスの移動ステップ数（整数値 1から5000） | 100 | 保存 |
| +1 | 左寄せ | 接続 / 切断 | トグルボタン | 接続中は Disconnect、未接続時は Connect を表示する | Connect |  |
| 0 | 中央 | Edit Mapping | ボタン | "抽象化操作名とデフォルトマッピング" を基に、別ウィンドウで DPAD / ボタン / スティックのマッピングを編集する。 |  |  |
| 0 | 右寄せ | 閉じる | ボタン | 受信側と切断しスクリプトを終了する。 | |
| +1 | 全体| コンソール | テキスト（複数行可）| ログの表示欄。固定幅フォントを用いる。 | 空 | |

#### 3.2.1 GUI - Windows - Mapping Editor

- マッピング編集機能はメインのウィンドウとは別の Mapping Editor ウィンドウとして開く。
- Mapping Editor は "抽象化操作名とデフォルトマッピング" の設定を初期値とし、Reset to default ボタンで "抽象化操作名とデフォルトマッピング" に戻せる。
- 編集対象は現在選択されているゲームパッドで利用可能な DPAD / ボタン / スティック入力であり、未使用項目は Unassigned として扱う。
- 現在選択されているゲームパッドで使用できない  DPAD / ボタン / スティック入力 がある場合は Mapping Editor に表示しない。例えば ボタンが 6 までしかない場合は ボタン 7 以降を表示しないなどを意味する。
- Mapping Editor の保存ボタンで変更したマッピングを保存する。
- 保存されたマッピングは設定ファイルの action_mapping に保持される。

### 3.3 GUI - Linux

- Linux 側スクリプトのGUI定義表は以下の通り。
- ウィンドウタイトルは "INDIPAD HOST"。
- 下表の "設定ファイル" の列に "保存" と書かれている項目は、"Scan INDI" および "閉じる" を実行時に設定ファイルに保存し、次回起動時に設定ファイルに保存さえている値を初期値として使用する。

| 位置（行） | 位置（列） | 名称 | 種類 | 説明 | 初期値 | 設定ファイル |
| - | - | - | - | - | - | - |
| 0 | 左寄せ | マウント | ラベル+ドロップダウンリスト | ラベルと利用可能なマウント名 |  | 保存 |
| +1 | 左寄せ | フォーカサー | ラベル+ドロップダウンリスト | ラベルと利用可能なフォーカサー名 |  | 保存 |
| +1 | 左寄せ | フィルターホイール | ラベル+ドロップダウンリスト | ラベルと利用可能なフィルターホイール名 |  | 保存 |
| 0 | 右寄せ | フィルタースロット数 | 固定文字列 | フィルターホイールのスロット数。入力不可。 | フィルターホイール欄に値が選択されたタイミングでフィルターホイールのスロット数をINDIドライバーから取得し設定する。1から10までの整数値。 | 保存 |
| +1 | 左寄せ | ローテーター | ラベル+ドロップダウンリスト | ラベルと利用可能なローテーター名 |  | 保存 |
| +1 | 左寄せ | Scan INDI | ボタン | D-BUSで利用可能なデバイスを調べる。 |  |  |
| +1 | 左寄せ | リスニングIP / ポート | ラベル+2つのテキスト | リスニング IP アドレスと整数値 | 0.0.0.0 / 50007 | 保存 |
| +1 | 左寄せ | ハートビートのログ出力 | チェックボックス+ラベル | チェックボックスの後ろにラベル表示 | オフ | 保存 |
| 0 | 中央 | リスタート | ボタン | 待ち受け状態の停止と再開を行う。リスニングIP/ポートが変更されている場合はその値を利用する。 | |
| 0 | 右寄せ | 閉じる | ボタン | スクリプトを終了する。 | |
| +1 | 全体| コンソール | テキスト（複数行可）| ログの表示欄。固定幅フォントを用いる。 | 空 | |

## 4. ネットワークプロトコル

ネットワーク送信は「JSON over TCP」を採用する。  

- 通信方式: JSON over TCP
- 送信頻度: 30〜60Hz 程度
- フレーム形式: 1 メッセージ 1 入力状態

### 4.1 パケット定義

パケットは以下の JSONスキーマ の通り。

```json
TBD
```

## 5. 具体的な設計

### 5.1 Windows 側スクリプト

#### 動作概要

- ゲームパッド入力の取得
- ボタンイベントの押下判定
- 入力と抽象化操作の変換
- LAN 上の Linux へ送信
- 接続されているゲームパッドが複数ある場合は GUI で選択可能
- 入力と抽象化操作のマッピングのカスタマイズが可能
- ゲームパッドの種類毎に DPAD / ボタン / スティック の物理配置を定義ファイルに設定する。

#### 使用ライブラリ

- `pygame`: Windows でゲームパッド入力を取得する上で実用性が高い
- `pyside6`: Qt for Python で GUI 化する。
- `socket`: 通信
- `threading` または `asyncio`: 受信タスクと送信タスク分離
- `pygame`は以下コマンドで導入する。

```PowerShell
PS> python -m venv .venv
PS> .venv\Scripts\activate
(.venv) PS> pip install pygame
(.venv) PS> pip install pyside6
```

#### 実装方針

- ゲームパッドのジョイスティック状態を周期的に読み取る。
- 読み取った入力を抽象化操作に変換し送信する。
- 入力の変化がない場合は送信を抑制し、無駄な通信を避ける。
- 接続が切れた場合は自動再接続を試みる。

### 5.2 Linux 受信側スクリプト

#### 動作概要

- 抽象化操作を受け取る。
- KStars / Ekos / INDI の操作コマンドへ変換する。
- 安全制御（速度制限、モード切替、停止条件）を実装する。
- INDIドライバーごとの動作の差異を定義ファイルで設定可能

#### 使用ライブラリ

- `pyside6`: Qt for Python で GUI 化する。
- `dbus-next`: KStars/Ekos/INDI を D-BUS で操作する。
- `socket`: 通信
- `threading` または `asyncio`: 受信タスクと送信タスク分離
- 必要なライブラリは以下の手順で導入する。

```bash
$ python -m venv .venv
$ source venv/bin/activate
(.venv) $ pip install pyside6
(.venv) $ pip install dbus-next
```

#### 実装方針

- `socketserver` または `asyncio` の TCP server を用いる。
- 入力パケットを検証し、形式の不正がある場合は破棄する。
- 受信した値を制御マッピングに変換する。
- 変換後のコマンドを `INDI` API または `Ekos` 連携方式に送る。
- ハートビートにより接続の切断を監視する。

### KStars / Ekos / INDI の制御

- KStars / Ekos / INDI の制御は [KStars DBUS Interface](https://api.kde.org/legacy/kstars/html/group__DBusInterface.html) を利用する。

- 天文観測機器は主に `/KStars/INDI` で制御する。
- KStars の SkyMap の制御は `/KStars` を用いる。
- 現時点で EKos レベルでの制御は未定。

＊＊＊＊＊＊＊＊＊＊＊＊＊＊＊


## 7. 操作マッピング

- 送信側のゲームパッド入力は観測デバイスを直観的に利用できるようJSON定義ファイルで入力と抽象化操作のマッピングを持つ。
- このマッピングは固定ではなく利用者がカスタマイズ可能。  
- KStars / Ekos / INDI の操作名を抽象化する。
- 抽象化操作名を用いてボタンへのマッピングを行う。
- GUI ではメイン画面の Edit Mapping から別ウィンドウを開き、7.1 のデフォルト設定を元に DPAD / ボタン / スティックのマッピングを編集できる。
- Mapping Editor には Reset to default ボタンを設け、既定の 7.1 設定へ戻せる。
- マッピングは設定ファイルの action_mapping に保存され、次回起動時に復元する。

### 7.1 抽象化操作名とデフォルトマッピング

- ELECOM JC-U3712T でのデフォルトのマッピングは下表の通り。

| デバイス区分 | 抽象化操作名 | 操作内容 | GAMEPADのマッピング |
| - | - | - | - |
| マウント | MOUNT_NORTH | マウントの移動: 北 | DPAD DOWN |
| マウント | MOUNT_SOUTH | マウントの移動: 南 | DPAD UP |
| マウント | MOUNT_WEST | マウントの移動: 西 | DPAD LEFT |
| マウント | MOUNT_EAST | マウントの移動: 東 | DPAD RIGHT |
| マウント | MOUNT_STEP_UP | マウント移動ステップUP | ボタン5（L上） |
| マウント | MOUNT_STEP_DOWN | マウント移動ステップDOWN | ボタン7（L下） |
| マウント | MOUNT_STOP | マウント移動停止（Slew停止） | ボタン9 |
| フォーカサー | FOCUS_OUT | フォーカスOut | ボタン6（R上） |
| フォーカサー | FOCUS_IN | フォーカスIn | ボタン8（R下） |
| フォーカサー | FOCUS_STEP_UP | フォーカスステップUP（5→10→50→100→500） | ボタン2 |
| フォーカサー | FOCUS_STEP_DOWN | フォーカスステップDOWN（5←10←50←100←500） | ボタン1 |
| フォーカサー | FOCUS_STOP | フォーカシング停止 | ボタン10 |
| フィルターホイール | FILTERWHEEL_PREV | フィルターホイールのスロット番号を一つ減らす | ボタン11 |
| フィルターホイール | FILTERWHEEL_NEXT | フィルターホイールのスロット番号を一つ増やす | ボタン12 |
| ローテーター | CAA_ROTATE_COUNTER_CLOCKWISE | ローテーター反時計回り（角度減少方向の回転）。ボタンダウンまたはアップでCAA_ROTATE_COUNTER_CLOCKWISEを送信 | ボタン3 |
| ローテーター | CAA_ROTATE_CLOCKWISE | ローテーター時計回り（角度増加方向の回転）ボタンダウンまたはアップCAA_ROTATE_CLOCKWISEを送信 | ボタン4 |
| KStars SkyMap | SKYMAP_MOVE | SkyMap の移動 | 左スティック（上下左右で画面に対して上下左右の移動） |
| KStars SkyMap | SKYMAP_ZOOM | SkyMap のZoom In/Out | 右スティック（上下） |
| KStars SkyMap | SKYMAP_ROTATE | SkyMap の回転 | 右スティック（左右） |

### 7.2 受信側の抽象化操作と観測デバイスの制御

- 受信側では、抽象化操作を元に KStars / Ekos / INDI で観測デバイスを制御する。
- 抽象化操作に対する観測デバイスの制御用コードが Python で定義される。

### 7.3 抽象化操作

抽象化された操作は以下の通り。

#### マウント

- Slew開始
  - 送信側: DPADのDOWN/UP/LEFT/RIGHT が押されたときに抽象化操作 MOUNT_NORTH/SOUTH/WEST/EAST を送信する。
  - 受信側: MOUNT_NORTH/SOUTH/WEST/EAST を受信すると継続して Slew する。
- Slew停止
  - 送信側: DPADのDOWN/UP/LEFT/RIGHT が離されると抽象化操作 MOUNT_STOP を送信する。
  - 受信側: MOUNT_STOP を受信すると

| 抽象化操作名 | 動作 |
| - | - |
| MOUNT_NORTH | pressed が true でマウントのDEC軸を北向きへSlewし続ける。pressed が false で北向きのSlewを停止。 |
| MOUNT_SOUTH | pressed が true でマウントのDEC軸を南向きへSlewし続ける。pressed が false で南向きのSlewを停止。 |
| MOUNT_WEST | pressed が true でマウントのRA軸を西向きへSlewし続ける。pressed が false で西向きのSlewを停止。 |
| MOUNT_EAST | pressed が true でマウントのRA軸を東向きへSlewし続ける。pressed が false で東向きのSlewを停止。 |
| MOUNT_STEP_UP | Slewの移動速度を一段階スピードアップする。移動速度はマウントによる。 |
| MOUNT_STEP_DOWN | Slewの移動速度を一段階スローダウンする。移動速度はマウントによる。 |
| MOUNT_STOP | Slew中のマウントを停止させる。トラックの停止ではない。 |

#### フォーカサー

- 受信側: FOCUS_IN/FOCUS_OUT が送信されると現在のステップ分フォーカス位置を一回移動させる。継続しての移動ではない。
- 連続して移動させるには、対象のボタンを繰り返しDOWN/UPする。

| 抽象化操作名 | 動作 |
| - | - |
| FOCUS_IN | 現在のステップでフォーカスインする。送信側で保持するステップ数も合わせて送信する。 |
| FOCUS_OUT | 現在のステップでフォーカスアウトする。送信側で保持するステップ数も合わせて送信する。 |
| FOCUS_STEP_UP | フォーカスステップUP。ステップは 5→10→50→100→500 の順に増加する。送信側で保持するステップ数の変更のみ。受信側へ FOCUS_STEP_UP を送信しない。 |
| FOCUS_STEP_DOWN | フォーカスステップDOWN。ステップは 500→100→50→10→5 の順に減少する。送信側で保持するステップ数の変更のみ。受信側へ FOCUS_STEP_DOWN を送信しない。 |
| FOCUS_STOP | 実行中のフォーカスの移動を停止させる。 |

#### フィルターホイール

- 受信側: FILTERWHEEL_SLOT_DEC/FILTERWHEEL_SLOT_INC が送信されるとスロット番号を一つ減もしくは一つ増やすして回転させる。
- 連続して回転させるには、対象のボタンを繰り返しDOWN/UPする。

| 抽象化操作名 | 動作 |
| - | - |
| FILTERWHEEL_PREV | フィルターホイールのスロット番号を一つ減らす。1を下限とする。 |
| FILTERWHEEL_NEXT | フィルターホイールのスロット番号を一つ増やす。スロットの最大数を上限とする。 |

#### ローテーター

- 送信側: CAA_ROTATE_CLOCKWISE または CAA_ROTATE_COUNTER_CLOCKWISE がマッピングされたボタンが押されたときと離されたときに CAA_ROTATE_CLOCKWISE または CAA_ROTATE_COUNTER_CLOCKWISE を送信する。
- 受信側: CAA_ROTATER_CLOCKWISE/COUNTER_CLOCKWISE を受信し Pressed が true の場合にローテーターを回転させる。
- 受信側: CAA_ROTATER_CLOCKWISE/COUNTER_CLOCKWISE を受信し Pressed が false の場合にローテーターの回転ループを停止し、ローテーターの回転停止を実行する。
- 受信側: Pressed が false になるまで、最初の5回の回転角は1度、6回目から8回目までは5度、9回目からは10度とする。
- 受信側: Heartbeat が切れた場合はローテーターの回転を停止させる。

| 抽象化操作名 | 動作 |
| - | - |
| CAA_ROTATE_COUNTER_CLOCKWISE | ローテーターを反時計回りに回転させる（角度減少方向の回転） |
| CAA_ROTATE_CLOCKWISE | ローテーターを時計回りに回転させる（角度増加方向の回転） |


#### KStars SkyMap

- KStars の SkyMap の視野をアナログスティックで制御する。
- 送信側: スティックが倒されている場合に前回送信値から変化した場合は抽象化操作名とその倒されている量（x, y）もしくは(x)か(y)を送信する。
- 受信側: 抽象化操作名を受信した場合は合わせて受信した(x, y)の値を移動量として指定の操作を開始する。移動量が(0, 0)もしくは(0)の場合は当該操作を停止する。
- マウントやFOVは移動させない。

| 抽象化操作名 | 動作 |
| - | - |
| SKYMAP_MOVE | SkyMap の移動。上下左右で画面に対して上下左右の移動）。(x, y)が移動量として送信される。 |
| SKYMAP_ZOOM | SkyMap のZoom In/Out。右スティック（上下）。(y)が移動量として送信される。 |
| SKYMAP_ROTATE | SkyMap の回転。右スティック（左右）。(x)が移動量として送信される。 |

## 8. 安全性と運用上の考慮

### 8.1 安全制御

- マウントが動いているときに誤入力が来た場合、止まるまでの一貫したハンドリングを行う。
- 速度の絶対値を制限する。
- 接続が切断された場合はマウント/フォーカサー/ローテーター/SkyMapの動作を自動で停止する。
- 無効な入力値が来た場合は破棄してログに残す。

### 8.2 ネットワーク制御

- LAN 内の特定アドレスに接続する設定を持つ。
- 実運用時はアドレスやポートを設定ファイルで管理する。
- ローカル起動時のデバッグ用のログ出力を残す。

### 8.3 ログ

- 入力イベントの記録。
- 接続状態変化の記録。
- 制御コマンド実行時刻の記録。
- 失敗時のエラーコード出力。
- デバッグ用に送受信した JSON をそのまま表示する。

### 8.4 デバッグ出力の要件

#### Windows 側デバッグ出力

- ゲームパッド入力を取得した直後の値を表示する。
- 送信前の JSON をそのままコンソールまたはログへ出力する。
- 送信する JSON の形式は、実際の送信データと同一にする。

#### Linux 側デバッグ出力

- TCP 受信した JSON をそのまま表示する
- 何を受け取ったかを確認できるようにし、修正やデバッグ時にそのまま再利用できるようにする。
- 受信したデータの整合性チェックと合わせて、異常なパケットの内容もログに残す。

#### 出力の基本方針

- 送信側と受信側の出力は同じスキーマを持ち、比較しやすくする。
- 本番では詳細ログを抑制し、問題発生時にのみフル出力を有効化する。

## 9. テスト方針

### 9.1 単体テスト

- 送信側: ゲームパッド値の抽象化操作への変換が期待どおりであることを確認。
- 受信側: 抽象化操作に対応する観測デバイス操作コマンドが意図通りであることを確認。

### 9.2 通信テスト

- 送信側と受信側の接続の維持確認。
- 接続断時の再接続確認。
- パケット欠落時のハンドリング確認。

### 9.3 統合テスト

- Linux 上で制御コマンドを再現可能なモック環境で検証。
- INDI / Ekos の実機がある場合は、運用上のリスクを踏まえ、段階的にテストする
- 最初は低リスクな微調整操作から開始し、慎重に高リスク操作へ広げる。

## 10. 実装順序

1. Wi-Fi / LAN 接続の確認と通信の最小実装
2. Windows 側でゲームパッド入力を読み取る最小コードを実装
3. Linux 受信側で受信した値をログ化する最小受信機を実装
4. 入力値から制御コマンドへの変換ロジックを実装
5. KStars / Ekos / INDI との接続点を定義し、最小の制御を検証
6. 安全制御と再接続制御を追加
7. 操作性の改善とボタン配置の最適化
8. 本番向け設定の整理と起動方法の確立

なお、ステップ 3 は初期検証として Windows 上で localhost を用いて構成してもよく、最終的な検証は StellarMate または Ubuntu の Linux 環境へ移す。

## 11. 技術的な採用判断

### 11.1 採用する技術

- Python 3
- `pygame`（Windows でのゲームパッド入力）
- `socket` / `asyncio`（通信）
- `json`（データ交換）
- `logging`（ログ出力）
- `systemd` 相当（StellarMate の自動起動）

### 11.2 依存の最小化

- 外部ライブラリの使用は初期段階では最小限に留める
- 必要に応じて、インターフェイスの抽象化層を持たせる
- 実装上の差分を最小限にして、制御の変更をしやすくする

## 12. 将来拡張の可能性

以下の拡張は、初期版の後に検討する。

- 複数ゲームパッド対応
- 追加の操作モード（ドーム制御、フィルタ切替、露出設定など）
- ビジュアル設定画面の提供
- 既存の XInput / DirectInput の抽象化
- エラー時の自動復帰アルゴリズムの改善

## 13. ファイル形式

### 13.1 設定ファイル

#### remote_indipad_sender.json

#### remote_indipad_receiver.json

### 13.1 定義ファイル

#### gamepad_profiles.json

#### device_profiles.json

以上