# 開発方針書

## 1. 目的

本プロジェクトの目的は、次の2点を実現することである。

1. Windows に接続されたゲームパッド入力をネットワーク経由で StellarMate へ送信する。
2. StellarMate など Linux 上の Ekos または INDI を操作し、望遠鏡制御の入力を実行する。

本件は、Windows 環境でのゲームパッド操作性を維持しつつ、StellarMate 側で一貫した制御に変換することを重視する。特に、観測時の低遅延、小さな設定変更、簡易な運用を前提に設計する。

---

## 2. 成功条件

以下を達成できることを目標とする。

- Windows でゲームパッドの入力を継続的に取得できること。
- 取得した入力をネットワーク経由で Linux へ安全に送信できること。
- Linux 側で入力の解釈・制御コマンド変換を行えること。
- Ekos / INDI の観測操作を適切に実行できること。
- 低遅延かつ再接続可能な通信を実装できること。
- システムが起動しても自動的に動作できること。

---

## 3. 想定環境

### 3.1 Windows 側

- OS: Windows 10 / 11
- Python 3.10 以降を想定
- 対応入力: ELECOM JC-U3712T を対象とし、一般的な DirectInput / XInput 対応ゲームパッドも可能な範囲で対応する
- 実行形態: Python スクリプトの手動起動またはタスクスケジューラ連携

#### 3.1.1 対象ゲームパッドの入力構成

- デバイス: ELECOM JC-U3712T
- 入力機構: 十字キー 1 個
- アナログスティック: 2 個
- ボタン: 12 個
- 概要: 標準的な HID / Game Controller として認識される想定で、Python の `pygame.joystick` または Win32 上のゲームパッド API から取得する
- 実装上の前提: ボタンは `button_0` 〜 `button_11` のようなインデックス表現に正規化し、制御ロジック側では論理名へ変換する
- 接続デバイスの選択方針: ゲームパッドが 1 台のみ接続されている場合は自動選択し、対話入力を行わない。複数接続時は一覧を表示して選択させるか、`--device` で明示指定する

### 3.2 受信側 Linux 環境

受信側は、StellarMate のような Linux ベースの環境だけでなく、Ubuntu 上で KStars / INDI を実行している環境も対象とする。

- OS: StellarMate OS または Ubuntu Linux
- Python 3 環境
- KStars / Ekos / INDI が動作していることを前提とする
- 通信は LAN 上で実行される前提
- 受信側スクリプトは OS に依存しにくい構成にし、Linux 環境で共通動作させる

### 3.3 実行形態の基本方針

- 送信側: Windows 上でゲームパッド入力を取得し、ネットワークで送信する
- 受信側: Linux 環境で入力を受け、KStars / Ekos / INDI へ変換して制御する
- 実運用では StellarMate もしくは Ubuntu のどちらでも稼働可能とする

---

## 4. 全体構成

本プロジェクトは、以下の2本の Python スクリプトを中心に構成する。

1. Windows 側スクリプト
   - ゲームパッド入力の取得
   - 入力値の正規化
   - ネットワーク送信

2. Linux 受信側スクリプト
   - TCP / WebSocket 受信
   - 入力状態の解釈
   - KStars / Ekos / INDI への制御コマンド変換
   - 各種安全制御

この構成により、Windows では入力取得に特化し、Linux 受信側では制御ロジックと観測機器制御に特化させる。受信側は StellarMate だけでなく Ubuntu 上でも動作させることを前提とする。

---

## 5. 実装方式の検討

本プロジェクトでは、初期実装として「JSON over TCP」を採用する。理由は、LAN 内での制御に対して実装の簡便さ、デバッグしやすさ、再接続制御の容易さ、安定性の高さを総合的に優先できるためである。

### 5.1 採用方式: JSON over TCP

- 長所
  - 実装が簡潔でデバッグしやすい
  - 送受信の再試行や接続維持が容易
  - Python 標準ライブラリで十分に実装可能
  - 安定性が高く、LAN 内での制御に適する
  - 低遅延要求と実運用の安定性のバランスが良い

- 短所
  - UDP より遅延がやや大きい
  - 1 パケットごとのオーバーヘッドがある

- 判定
  - 本プロジェクトでは最も実用的な選択肢であり、初期実装の主設計として採用する
  - 低遅延と安定性の両立を重視し、制御品質よりも運用信頼性を優先する

### 5.2 採用方針

- 通信方式: JSON over TCP
- 送信頻度: 30〜60Hz
- フレーム形式: 1 メッセージ 1 入力状態
- パケット例:

```json
{
  "ts": 1690000000.123,
  "type": "axis",
  "device": "gamepad",
  "axes": {
    "left_x": 0.12,
    "left_y": -0.45,
    "right_x": 0.0,
    "right_y": 0.0
  },
  "buttons": {
    "a": true,
    "b": false,
    "x": false,
    "y": false
  },
  "mode": "slew"
}
```

この方式は、制御値とイベントを単純に追跡可能で、将来的なデバッグや中継機能の追加にも適している。

---

## 6. 具体的な設計

### 6.1 Windows 側スクリプト

#### 役割

- ゲームパッド入力の取得
- 入力の正規化（-1.0 〜 1.0）
- ボタンイベントの押下判定
- モード切替とコマンド変換
- LAN 上の StellarMate へ送信
- 接続されているゲームパッドが 1 台のみの場合は自動選択し、対話入力を省略する
- 複数台ある場合は利用可能なゲームパッドを一覧表示し、選択または `--device` 指定で明示する

#### 推奨ライブラリ

- `pygame`: Windows でゲームパッド入力を取得する上で実用性が高い
- `socket`: 通信
- `threading` または `asyncio`: 受信タスクと送信タスク分離

#### 実装方針

- ゲームパッドのジョイスティック状態を周期的に読み取る
- 軸の値をスムージングまたは閾値処理する
- 連続入力とバイナリ入力を分離して管理する
- 入力の変化がない場合は送信を抑制し、無駄な通信を避ける
- 接続が切れた場合は自動再接続を試みる

#### 重要な設計判断

- Windows 側は「入力取得と通信送信」に集中する
- 変更のたびに複雑な制御ロジックを持たせない
- すべてを StellarMate 側で解釈させる設計とする

### 6.2 Linux 受信側スクリプト

#### 役割

- ゲームパッド入力を受け取る
- 制御モードを解釈する
- KStars / Ekos / INDI の操作コマンドへ変換する
- 安全制御（速度制限、モード切替、停止条件）を実装する

#### 実装方針

- `socketserver` または `asyncio` の TCP server を用いる
- 入力パケットを検証し、形式の不正がある場合は破棄する
- 受信した値を制御マッピングに変換する
- 変換後のコマンドを `INDI` API または `Ekos` 連携方式に送る
- 環境差異（StellarMate / Ubuntu）を吸収するアダプタ層を持たせる

#### 検討対象の制御への接続方法

- INDI への接続
  - `indiserver` / INDI client API への接続を前提にする
  - 可能であれば Python から INDI のドライバ・プロパティにアクセスする

- Ekos への接続
  - Ekos の制御 API やローカル連携インターフェイスを利用する
  - その場合は INDI に依存しない制御経路を構築できる

- KStars / StellarMate / Ubuntu 共通の設計
  - 受信側は KStars/Ekos/INDI への呼び出しを抽象化し、Linux 上で共通利用できるようにする
  - StellarMate ではそのまま利用し、Ubuntu でも同じ入力変換ロジックと安全制御を再利用する

- 判定
  - 初期実装では INDI の制御メッセージを優先し、Ekos は代替またはラッパーとして扱うのが現実的

### 6.3 Ekos / INDI の操作方法の検討

Ekos と INDI をどこから制御するかは、運用の安定性と保守性に大きく影響する。以下の方法を比較し、初期実装では INDI の制御を中心に据えるのが最も現実的である。

#### 方法 1: INDI server へ直接接続する

- 概要
  - Python 受信側が `indiserver` へ TCP 接続し、INDI の XML ベースのメッセージを送受信する
  - 機器のプロパティや命令を直接制御する

- 長所
  - KStars / Ekos の GUI 依存が少ない
  - 望遠鏡制御の中心は INDI であるため、制御の本体に近い
  - 実機運用と相性がよく、観測制御ロジックを安定させやすい

- 短所
  - INDI のプロトコル仕様の理解が必要
  - プロパティ名や命令が機器ごとに異なることがある
  - 変換ロジックの実装がやや複雑になる

- 判定
  - 初期実装の主経路として推奨

#### 方法 2: Ekos の自動化インターフェイスを利用する

- 概要
  - KStars / Ekos に備わる自動化機能や、ローカル連携機構を利用して操作する

- 長所
  - KStars の観測機能と親和性が高い
  - 既存の観測シーケンスとの統合がしやすい

- 短所
  - 公式の Python API が明確でない場合が多い
  - GUI 依存やバージョン差分が入りやすい
  - 自動化の安定性が INDI ほど明確ではない

- 判定
  - 代替案としては有効だが、初期実装の主戦略ではない

#### 方法 3: KStars の GUI 操作を OS レベルで制御する

- 概要
  - キーボード操作やマウス操作で KStars / Ekos を直接制御する

- 長所
  - 実装が可能な場合がある
  - 既存の UI をそのまま利用できる

- 短所
  - 非常に壊れやすい
  - GUI のレイアウト変更に対して脆弱
  - 実運用に耐えない

- 判定
  - 推奨しない

#### 推奨方針

- 受信側の制御の第一候補は INDI server への直接接続である
- その上で、予備として KStars / Ekos 側の連携方法を検討する
- 受信側の JSON 定義ファイルで、各入力を INDI の property / command に変換できるようにする

この設計により、制御の責務を「入力収集」と「制御実行」に分離し、環境差異や更新頻度の違いにも対応しやすくなる。

---

## 7. 操作マッピング方針

ゲームパッド入力は、観測制御に直結するように以下の用途に分ける。

### 7.1 主要な軸マッピング

- 左スティック X: 望遠鏡の左右移動（RA / Azimuth）
- 左スティック Y: 望遠鏡の上下移動（Dec / Altitude）
- 右スティック X: 追尾 / 微調整
- 右スティック Y: ズームまたは焦点微調整
- トリガー: 速度の増減（高速/低速）

### 7.2 対象ゲームパッドの入力構成とマッピング

ELECOM JC-U3712T は、以下の入力構成を前提とする。

- 十字キー: モード切替、速度選択、対象選択
- 左アナログスティック: RA / Azimuth 制御
- 右アナログスティック: Dec / Elevation 制御または微調整
- ボタン x12: 各ボタンをインデックス管理し、後段のロジックで現実的な機能に割り当てる

#### 7.2.1 ボタンの正規化

- Python 側では `button_1` 〜 `button_12` の番号として扱う
- 運用時には、論理名へマッピングする
- 例: `button_1` = A, `button_2` = B, `button_3` = X, `button_4` = Y などのように、実際の物理ボタン配置に応じて再定義する

#### 7.2.2 論理マッピング例

- A / B: 追尾開始・停止、リセット操作
- X / Y: フォーカス調整、モード切替
- START / BACK 相当: 測定開始 / 記録開始、モード戻し、停止
- 十字キー: 速度モード切替、対象選択
- 12 本のボタンを、初期実装では 4 つの主要操作 + 8 つの補助操作として割り当てる

> ここでのボタン名は論理名であり、ELECOM JC-U3712T の物理ボタンの実態に応じてマッピングを調整する。

### 7.3 受信側の JSON 定義ファイルによる機能マッピング

受信側では、ゲームパッド入力の意味づけと Ekos / INDI の各機能への対応を JSON ファイルで定義する。

- 受信側は JSON 定義ファイルを読み込み、各入力に対する機能を決定する
- 実行時にマッピングを切り替えられるようにし、個別環境差分を JSON で吸収する
- 受信側が持つ「入力 -> 機能」定義は、Windows 側の送信フォーマットとは独立に管理する

#### 例: 定義ファイルの形式

```json
{
  "device": "ELECOM JC-U3712T",
  "version": 1,
  "axes": {
    "left_x": { "target": "mount.ra_rate", "type": "continuous" },
    "left_y": { "target": "mount.dec_rate", "type": "continuous" },
    "right_x": { "target": "focus.adjust", "type": "continuous" },
    "right_y": { "target": "zoom.adjust", "type": "continuous" }
  },
  "buttons": {
    "button_1": { "target": "tracking.toggle", "type": "toggle" },
    "button_2": { "target": "mount.stop", "type": "action" },
    "button_3": { "target": "focus.increase", "type": "action" },
    "button_4": { "target": "focus.decrease", "type": "action" },
    "button_5": { "target": "camera.capture", "type": "action" },
    "button_6": { "target": "mode.slew", "type": "mode" }
  },
  "dpad": {
    "up": { "target": "speed.increase", "type": "action" },
    "down": { "target": "speed.decrease", "type": "action" },
    "left": { "target": "target.previous", "type": "action" },
    "right": { "target": "target.next", "type": "action" }
  }
}
```

#### 7.3.1 定義ファイルの管理方針

- 受信側の設定ファイルは `mapping.json` のような命名を推奨する
- 設定ファイルを環境ごとに置き換えられるようにする
- 例: `mapping_stellarmate.json`, `mapping_ubuntu.json`
- JSON の検証を行い、不正なキーや未定義の機能名を検出する

#### 7.3.2 実装時の責務分離

- Windows 側: 物理ゲームパッドの値を送信する
- Linux 受信側: 受信した値と JSON 定義ファイルを照合し、実際の Ekos / INDI コマンドへ変換する
- これにより、送信データのフォーマットと実際の制御機能を分離して保守しやすくする

### 7.3 速度制御

- 通常: 低速
- 連射またはトリガー押下: 中速
- 連続で高い入力が維持: 高速

- 速度オーバーを避けるため、制限係数を適用する
- 物理制御入力には死区を設ける

---

## 8. 安全性と運用上の考慮

### 8.1 安全制御

- 望遠鏡が動いているときに誤入力が来た場合、止まるまでの一貫したハンドリングを行う
- 速度の絶対値を制限する
- 接続が切断された場合は自動で動作を停止する
- 無効な入力値が来た場合は破棄してログに残す

### 8.2 ネットワーク制御

- LAN 内の特定アドレスに接続する設定を持つ
- 実運用時はアドレスやポートを設定ファイルで管理する
- ローカル起動時のデバッグ用のログ出力を残す

### 8.3 ログ

- 入力イベントの記録
- 接続状態変化の記録
- 制御コマンド実行時刻の記録
- 失敗時のエラーコード出力
- デバッグ用に送受信した JSON をそのまま表示する

### 8.4 デバッグ出力の要件

#### Windows 側デバッグ出力

- ゲームパッド入力を取得した直後の値を表示する
- 送信前の JSON をそのままコンソールまたはログへ出力する
- 送信する JSON の形式は、実際の送信データと同一にする
- 例: 受信側の解析に必要なキー `axes`, `buttons`, `mode`, `ts` をそのまま出力する

#### Linux 側デバッグ出力

- TCP 受信した JSON をそのまま表示する
- 何を受け取ったかを確認できるようにし、修正やデバッグ時にそのまま再利用できるようにする
- 受信したデータの整合性チェックと合わせて、異常なパケットの内容もログに残す

#### 出力の基本方針

- デバッグ時は 1 行 1 JSON として表示する
- 送信側と受信側の出力は同じスキーマを持ち、比較しやすくする
- 本番では詳細ログを抑制し、問題発生時にのみフル出力を有効化する

---

## 9. テスト方針

### 9.1 単体テスト

- ゲームパッド値の正規化が期待どおりであることを確認
- 速度制限とデッドゾーン処理を確認
- 各モードのマッピングが意図通りであることを確認

### 9.2 通信テスト

- Windows → StellarMate の接続の維持確認
- 接続断時の再接続確認
- パケット欠落時のハンドリング確認

### 9.3 統合テスト

- StellarMate 上で制御コマンドを再現可能なモック環境で検証
- INDI / Ekos の実機がある場合は、運用上のリスクを踏まえ、段階的にテストする
- 最初は低リスクな微調整操作から開始し、慎重に高リスク操作へ広げる

---

## 10. 実装順序

1. Wi-Fi / LAN 接続の確認と通信の最小実装
2. Windows 側でゲームパッド入力を読み取る最小コードを実装
3. Linux 受信側で受信した値をログ化する最小受信機を実装
4. 入力値から制御コマンドへの変換ロジックを実装
5. KStars / Ekos / INDI との接続点を定義し、最小の制御を検証
6. 安全制御と再接続制御を追加
7. 操作性の改善とボタン配置の最適化
8. 本番向け設定の整理と起動方法の確立

なお、ステップ 3 は初期検証として Windows 上で localhost を用いて構成してもよく、最終的な検証は StellarMate または Ubuntu の Linux 環境へ移す。

---

## 11. 技術的な採用判断

### 11.1 採用する技術

- Python 3
- `pygame`（Windows でのゲームパッド入力）
- `socket` / `asyncio`（通信）
- `json`（データ交換）
- `logging`（ログ出力）
- `systemd` 相当（StellarMate の自動起動）

### 11.2 依存の最小化

- 外部ライブラリの使用は初期段階では最小限に留める
- 必要に応じて、インターフェイスの抽象化層を持たせる
- 実装上の差分を最小限にして、制御の変更をしやすくする

---

## 12. 将来拡張の可能性

以下の拡張は、初期版の後に検討する。

- 複数ゲームパッド対応
- 追加の操作モード（ドーム制御、フィルタ切替、露出設定など）
- ビジュアル設定画面の提供
- 既存の XInput / DirectInput の抽象化
- エラー時の自動復帰アルゴリズムの改善

---

## 13. 結論

本プロジェクトにおいては、Windows 側での入力取得と Linux 受信側での制御コマンド変換を明確に分離する設計が最も適している。両者の責務を整理し、通信方式は安定性重視の JSON over TCP とし、制御ロジックは KStars / Ekos / INDI を実行する Linux 側に集約することで、実用性と保守性を両立できる。

この方針により、ゲームパッドを観測制御の入力デバイスとして使いながら、StellarMate または Ubuntu 上の Ekos / INDI を安全かつ一貫して実行することが可能になる。
